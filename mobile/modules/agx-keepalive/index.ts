// No JS surface: this module is reached through
// requireOptionalNativeModule("AgxKeepAlive") in
// src/notifications/keepAlive.ts, the same way any other Expo native module
// is, rather than through an import of this package. This file exists only
// so the package has a valid "main" for the workspace link.
export {};
